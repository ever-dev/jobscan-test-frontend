/*
 * API URLS should be named as a type followed by modifier
 */
export const API_URL = 'http://localhost:8000/api'

const URLS = {
  getSkills: '/skills',
  getJobPosts: '/postings/search/skills',
}

export default URLS
